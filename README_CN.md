# 🤖 语音引导机械臂智能分拣系统

<div align="center">

[![License](https://img.shields.io/badge/license-Apache%202.0-blue.svg)](LICENSE)
[![Python](https://img.shields.io/badge/python-3.8+-blue.svg)](https://www.python.org/downloads/)
[![ROS2](https://img.shields.io/badge/ROS2-Humble-blue.svg)](https://docs.ros.org/en/humble/)
[![YOLO](https://img.shields.io/badge/YOLO-v3--v11-yellow.svg)](https://github.com/ultralytics/ultralytics)

**集成语音识别、计算机视觉和机械臂控制的智能分拣系统**

[English](README.md) | [中文](README_CN.md)

</div>

---

## 📖 项目简介

这是一个集成了**语音识别**、**视觉检测**和**机械臂控制**的智能分拣系统。系统通过自然语言指令,能够识别、定位并抓取指定物品,实现自动化分拣。

**支持物品**: 苹果🍎 | 橘子🍊 | 杯子☕ | 瓶子🍾

## ✨ 核心特性

- 🎯 **语音到动作全流程**: 说话 → 检测 → 抓取 → 分拣
- 🚀 **硬件加速**: 支持NPU(昇腾310B) / GPU(CUDA)
- 🔄 **多种运行模式**: 仅视觉测试 / 单次执行 / 持续运行
- 📦 **模块化架构**: 易于扩展和定制
- 🛠️ **生产就绪**: 完善的日志和错误处理

## 🎬 工作流程演示

```
用户: "帮我拿一个苹果"
  ↓
[语音识别] → 转换为文本
  ↓
[LLM解析] → 提取意图: target="苹果"
  ↓
[YOLO检测] → 在摄像头画面中定位苹果
  ↓
[坐标映射] → 像素坐标 → 机械臂基坐标
  ↓
[逆运动学] → 计算关节角度
  ↓
[机械臂执行] → 抓取 → 移动到分拣区 → 释放
```

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                    用户语音指令                              │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌──────────────────────────────────────────────────────────────┐
│  语音识别模块 (recognize_voice.py)                            │
│  ├─ 音频输入(麦克风)                                          │
│  └─ 语音转文字                                                │
└──────────────────────┬───────────────────────────────────────┘
                       ↓
┌──────────────────────────────────────────────────────────────┐
│  LLM意图解析 (LLM意图识别.py)                                 │
│  ├─ 自然语言理解                                              │
│  └─ 目标物品提取                                              │
└──────────────────────┬───────────────────────────────────────┘
                       ↓
┌──────────────────────────────────────────────────────────────┐
│  视觉感知模块 (VisionPerception类)                            │
│  ├─ YOLO检测 (支持v3-v11)                                    │
│  ├─ 后端: NPU/GPU/CPU自动选择                                │
│  └─ 物体定位                                                  │
└──────────────────────┬───────────────────────────────────────┘
                       ↓
┌──────────────────────────────────────────────────────────────┐
│  决策层 (DecisionLayer类)                                     │
│  └─ 匹配语音目标与视觉检测结果                                │
└──────────────────────┬───────────────────────────────────────┘
                       ↓
┌──────────────────────────────────────────────────────────────┐
│  坐标映射 (CoordinateMapper类)                                │
│  └─ 像素坐标 → 机械臂基坐标系                                │
└──────────────────────┬───────────────────────────────────────┘
                       ↓
┌──────────────────────────────────────────────────────────────┐
│  机械臂控制 (RobotArmController类)                            │
│  ├─ ROS2服务: 逆运动学求解 (trial_service)                   │
│  ├─ 运动规划: MoveIt2                                        │
│  └─ 执行: 抓取 → 运输 → 释放                                 │
└──────────────────────────────────────────────────────────────┘
```

**目录结构**:
```
├── mindyolo-master/              # YOLO模型训练与推理
│   ├── configs/                  # 模型配置 (YOLOv3-v11)
│   ├── demo/                     # 演示脚本
│   │   ├── LLM意图识别.py        # LLM集成
│   │   ├── recognize_voice.py    # 语音识别
│   │   └── npupredict.py         # NPU加速推理
│   └── deploy/                   # 模型部署工具
│
├── ros2_robot_arm/               # 机械臂控制系统
│   └── ros2_ws/                  # ROS2工作空间
│       └── src/
│           ├── dofbot_moveit/    # 运动规划(逆运动学服务器)
│           ├── dofbot_info/      # 机器人消息和服务
│           └── dofbot_garbage_yolov5/  # 旧版检测模块
│
├── voice_guided_robot_system.py  # 主程序
├── debug_check.py                # 诊断工具
└── run_ubuntu.sh                 # 启动脚本
```

## 🔧 环境要求

### 硬件要求
- **操作系统**: Ubuntu 20.04 / 22.04 (推荐)
- **机械臂**: Dofbot 5自由度机械臂或兼容型号
- **摄像头**: USB/CSI摄像头 (640×480或更高)
- **麦克风**: 用于语音输入
- **可选**: 华为昇腾NPU (310B) 用于加速

### 软件要求
| 组件 | 版本 | 必需 |
|------|------|------|
| Python | 3.8+ | ✅ |
| ROS2 | Humble | ✅ |
| PyTorch | 1.10+ | ✅ (CPU/GPU) |
| Ultralytics | 最新版 | ✅ |
| OpenCV | 4.x | ✅ |
| MindSpore | 2.x | ⚪ (NPU使用) |
| CUDA | 11.x+ | ⚪ (GPU使用) |

## 🚀 快速开始

### 1️⃣ 安装依赖

```bash
# 克隆仓库
git clone https://github.com/YOUR_USERNAME/voice-guided-robot-sorting.git
cd voice-guided-robot-sorting

# 安装Python包
pip3 install -r requirements.txt

# 安装ROS2 Humble (如果未安装)
# Ubuntu 22.04:
sudo apt update
sudo apt install ros-humble-desktop

# 安装机械臂控制库
cd ros2_robot_arm/0.py_install
pip3 install .
```

### 2️⃣ 配置API密钥

**重要**: 运行系统前需要配置API凭证。

```bash
# 复制环境变量示例文件
cp .env.example .env

# 编辑.env并填入你的API密钥:
# - XFYUN_APPID: 从 https://console.xfyun.cn/ 获取
# - XFYUN_API_KEY: 科大讯飞语音识别API密钥
# - XFYUN_API_SECRET: 科大讯飞语音识别密钥
# - DOUBAO_API_KEY: 从 https://console.volcengine.com/ark 获取
```

**加载环境变量**:

```bash
# Linux/Mac
source .env
# 或
export $(cat .env | xargs)

# Windows PowerShell
Get-Content .env | ForEach-Object {
    $var = $_.Split('=')
    [Environment]::SetEnvironmentVariable($var[0], $var[1])
}
```

> **🔒 安全提示**: 永远不要将`.env`文件提交到Git! 该文件已在`.gitignore`中。

### 3️⃣ 构建ROS2工作空间

```bash
cd ros2_robot_arm/ros2_ws

# 加载ROS2环境
source /opt/ros/humble/setup.bash

# 构建工作空间
colcon build --symlink-install

# 加载工作空间
source install/setup.bash
```

> **⚠️ 重要**: 构建前务必先运行 `source /opt/ros/humble/setup.bash`!

### 4️⃣ 启动系统

**方式A: 仅视觉检测模式** (无需机械臂)
```bash
./run_ubuntu.sh
# 选择 [1] 仅视觉检测
```

**方式B: 完整系统** (需要机械臂硬件)

*终端1 - 启动ROS2服务:*
```bash
source /opt/ros/humble/setup.bash
cd ros2_robot_arm/ros2_ws
source install/setup.bash
ros2 run dofbot_moveit dofbot_server
```

*终端2 - 运行主程序:*
```bash
source /opt/ros/humble/setup.bash
cd ros2_robot_arm/ros2_ws
source install/setup.bash
cd ../../
python3 voice_guided_robot_system.py
```

**方式C: 使用启动脚本**
```bash
chmod +x run_ubuntu.sh
./run_ubuntu.sh
# 选择模式: [1] 仅视觉 | [2] 单次运行 | [3] 持续运行
```

## 💬 使用示例

### 语音指令 (自然语言)

```
用户: "帮我拿一个苹果"          → 系统: 检测并抓取苹果
用户: "抓取橘子"                → 系统: 检测并抓取橘子
用户: "I need a cup"            → 系统: 检测并抓取杯子
用户: "把瓶子放到篮子里"        → 系统: 检测并抓取瓶子
```

**支持语言**: 中文、English

### 支持的物品类别

| 物品 | 中文 | 英文 | COCO类别 |
|------|------|------|---------|
| 🍎 | 苹果 | Apple | `apple` |
| 🍊 | 橘子/橙子 | Orange | `orange` |
| ☕ | 杯子/水杯 | Cup | `cup` |
| 🍾 | 瓶子 | Bottle | `bottle` |

### 分拣位置配置

每个物品分拣到预定义位置:
```python
SORTING_POSITIONS = {
    "apple":  位置1 (左前)   # [45°, 50°, 20°, 60°, 265°]
    "orange": 位置2 (左后)   # [27°, 75°, 0°, 50°, 265°]
    "cup":    位置3 (右后)   # [147°, 75°, 0°, 50°, 265°]
    "bottle": 位置4 (右前)   # [133°, 50°, 20°, 60°, 265°]
}
```

## ⚙️ 配置说明

### 系统配置 (`system_config.py`)

```python
# 摄像头设置
CAMERA_ID = 0              # USB摄像头索引
RESOLUTION = (640, 480)    # 图像尺寸

# 检测参数
CONF_THRESHOLD = 0.5       # 置信度阈值
IOU_THRESHOLD = 0.65       # NMS IoU阈值

# 机械臂设置
ROBOT_SPEED = 1000         # 舵机速度 (ms)
GRASP_ANGLE = 130          # 夹爪闭合角度

# 加速设置
DEVICE = "auto"            # auto | npu | cuda | cpu
```

### YOLO模型选择

`mindyolo-master/configs/` 中支持的模型:
- **YOLOv3** - 经典基线
- **YOLOv5** - 快速准确 (s/m/l/x变体)
- **YOLOv7** - 高性能
- **YOLOv8** - Ultralytics最新版 (推荐)
- **YOLOv9/v10/v11** - 前沿版本
- **YOLOX** - 无锚框替代方案

切换模型,编辑 `voice_guided_robot_system.py`:
```python
config = {
    "model_path_pt": "path/to/yolov8s.pt",  # 在此更改模型
    # ...
}
```

## 🔍 故障排查

<details>
<summary><b>❌ ROS2服务超时</b></summary>

**错误**: `❌ [失败] 逆运动学服务超时(5秒内未响应)`

**解决方案**: 启动逆运动学服务:
```bash
source /opt/ros/humble/setup.bash
cd ros2_robot_arm/ros2_ws && source install/setup.bash
ros2 run dofbot_moveit dofbot_server
```

验证服务运行:
```bash
ros2 service list | grep trial_service
```
</details>

<details>
<summary><b>❌ 模块导入错误</b></summary>

**错误**: `ModuleNotFoundError: No module named 'mindyolo'`

**解决方案**: 安装mindyolo包:
```bash
cd mindyolo-master
pip3 install -e .
```
</details>

<details>
<summary><b>❌ ROS2构建失败</b></summary>

**错误**: `Could not find a package configuration file provided by "ament_cmake"`

**解决方案**: 构建前加载ROS2环境:
```bash
source /opt/ros/humble/setup.bash
cd ros2_robot_arm/ros2_ws
colcon build --symlink-install
```
</details>

<details>
<summary><b>❌ 摄像头未找到</b></summary>

**错误**: `❌ 无法打开摄像头`

**解决方案**: 
1. 检查摄像头连接: `ls /dev/video*`
2. 测试摄像头: `cheese` 或 `v4l2-ctl --list-devices`
3. 更改配置中的摄像头ID: `CAMERA_ID = 1` (尝试 0, 1, 2...)
</details>

<details>
<summary><b>❌ 语音识别不工作</b></summary>

**解决方案**:
- 检查麦克风权限
- 测试麦克风: `arecord -d 5 test.wav && aplay test.wav`
- 安装音频库: `sudo apt install portaudio19-dev`
</details>

<details>
<summary><b>🛠️ 诊断工具</b></summary>

运行自动系统检查:
```bash
python3 debug_check.py
```

检查项目:
- ✅ ROS2环境
- ✅ 服务可用性
- ✅ 串口连接
- ✅ Python依赖
</details>

## 🧪 开发指南

### 测试

```bash
# 仅测试视觉检测
python3 test_4class_detection.py

# 测试完整系统集成
python3 test_integration.py

# 运行诊断检查
python3 debug_check.py
```

### 模型训练

训练自定义YOLO模型:
```bash
cd mindyolo-master

# 准备数据集 (COCO格式)
# 编辑配置: configs/yolov8/yolov8s.yaml

# 训练模型
python train.py --config configs/yolov8/yolov8s.yaml \
                --data configs/coco.yaml \
                --epochs 100

# 导出模型用于部署
python export.py --config configs/yolov8/yolov8s.yaml \
                 --weight runs/train/weights/best.ckpt
```

详见 `mindyolo-master/GETTING_STARTED.md`。

### 添加新物品类别

1. **更新映射** 在 `voice_guided_robot_system.py` 中:
```python
OBJECT_MAPPING = {
    "苹果": "apple",
    "你的物品": "coco_class_name",  # 在此添加
}
```

2. **添加分拣位置**:
```python
SORTING_POSITIONS = {
    "coco_class_name": [j1, j2, j3, j4, j5],  # 关节角度
}
```

3. **更新LLM提示词** 在 `demo/LLM意图识别.py` 中

## 🛠️ 技术栈

| 层次 | 技术 |
|------|------|
| **视觉** | YOLO v3-v11, YOLOX (MindSpore/PyTorch/Ultralytics) |
| **语音** | 语音识别库 |
| **NLP** | 大语言模型(LLM)集成 |
| **机械臂控制** | ROS2 Humble + MoveIt2 |
| **运动学** | Orocos KDL |
| **加速** | 华为昇腾NPU / NVIDIA CUDA |
| **框架** | Python 3.8+, C++ 14 |

## 📚 相关文档

- [`ARCHITECTURE.txt`](ARCHITECTURE.txt) - 系统架构详情
- [`SYSTEM_USAGE.txt`](SYSTEM_USAGE.txt) - 使用指南
- [`UBUNTU_SETUP.txt`](UBUNTU_SETUP.txt) - Ubuntu安装说明
- [`完整部署指南.txt`](完整部署指南.txt) - 完整部署指南
- [`4类物品分拣系统更新说明.md`](4类物品分拣系统更新说明.md) - 更新日志

## 🤝 贡献

欢迎贡献! 请:
1. Fork 本仓库
2. 创建特性分支: `git checkout -b feature/AmazingFeature`
3. 提交更改: `git commit -m 'Add AmazingFeature'`
4. 推送到分支: `git push origin feature/AmazingFeature`
5. 提交Pull Request

## 📄 许可证

本项目采用 Apache 2.0 许可证 - 详见 [LICENSE](LICENSE) 文件。

## 🙏 致谢

- [Ultralytics YOLO](https://github.com/ultralytics/ultralytics) - 目标检测模型
- [MindSpore](https://www.mindspore.cn/) - 深度学习框架
- [ROS2](https://docs.ros.org/) - 机器人操作系统
- [Orocos KDL](https://www.orocos.org/kdl.html) - 运动学库

## 📧 联系方式

- **问题反馈**: [GitHub Issues](https://github.com/YOUR_USERNAME/voice-guided-robot-sorting/issues)
- **讨论交流**: [GitHub Discussions](https://github.com/YOUR_USERNAME/voice-guided-robot-sorting/discussions)

---

<div align="center">

**⭐ 如果这个项目对你有帮助,请给个Star! ⭐**

**💡 首次使用建议: 先使用仅视觉检测模式验证检测功能,再启用完整系统**

Made with ❤️ by the Robot Vision Team

</div>
